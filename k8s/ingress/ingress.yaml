apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
   name: gocore-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: graphql
            port:
              number: 8080