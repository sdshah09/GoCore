# Copy this file to values-secret.yaml and set your credentials.
# values-secret.yaml is gitignored and will not be committed.
#
#   cp values-secret.yaml.example values-secret.yaml
#   # edit values-secret.yaml, then:
#   helm install gocore . -f values.yaml -f values-secret.yaml
#
secrets:
  dbCredentials:
    name: db-credentials
    stringData:
      postgres-user: "postgres"
      postgres-password: "CHANGE_ME"
